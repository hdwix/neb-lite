export enum ERideStatus {
  NYSTART = 'ny_start',
  REQUESTED = 'requested',
  CANDIDATES_COMPUTED = 'candidates_computed',
  ASSIGNED = 'assigned',
  ACCEPTED = 'accepted',
  ENROUTE = 'enroute',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}
