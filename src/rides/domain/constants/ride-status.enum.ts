export enum ERideStatus {
  NYSTART = 'ny_start',
  REQUESTED = 'requested',
  CANDIDATES_COMPUTED = 'candidates_computed',
  ASSIGNED = 'assigned',
  ACCEPTED = 'accepted',
  ENROUTE = 'enroute',
  TRIP_STARTED = 'trip_started',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}
